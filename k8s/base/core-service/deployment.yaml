apiVersion: apps/v1
kind: Deployment
metadata:
  name: core-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: core-service
  template:
    metadata:
      labels:
        app: core-service
    spec:
      containers:
        - name: core-service
          image: detailsnap/core-service
          ports:
            - containerPort: 8082
          envFrom:
            - secretRef:
                name: app-credentials
          env:
            - name: DB_NAME
              value: detailsnap
            - name: CORS_ORIGIN
              value: http://localhost:5173
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8082
            initialDelaySeconds: 10
            periodSeconds: 10
