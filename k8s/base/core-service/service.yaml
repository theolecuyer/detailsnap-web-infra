apiVersion: v1
kind: Service
metadata:
  annotations:
    alb.ingress.kubernetes.io/target-type: ip
  name: core-service
spec:
  selector:
    app: core-service
  ports:
    - port: 8082
      targetPort: 8082
