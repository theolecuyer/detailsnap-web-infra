apiVersion: v1
kind: Service
metadata:
  annotations:
    alb.ingress.kubernetes.io/target-type: ip
  name: media-service
spec:
  selector:
    app: media-service
  ports:
    - port: 8083
      targetPort: 8083
